/* const express = require(express)
const app  = express()

const path = [
    '/arch',
    '/cpu',
    '/hostname',
    '/homedir',
    '/network',
    '/plataform',
    '/uptime',
    '/userinfo'
]

const changeBg = function (req, res, next) {
    
    next()
  }
  
  app.use(myLogger)

if (path.includes(window.location.pathname)) alert('aa') */